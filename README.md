## Create Your Own React 19 Project with Vite and TailwindCSS
This repository can be used in multiple ways to set up your own project. The most straightforward approach is to execute the following command in the terminal:

```bash
npx degit StayTunedWithSuez/vite-react-tailwind#main <YOUR_PROJECT_NAME>
```
